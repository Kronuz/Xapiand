Let's try this.

Continue the Xapiand → Kronuz/http inversion (Leg 2, architecture B), now at #51 stage 3. Read ~/Development/Kronuz/HANDOFF-http-server-lib.md (especially the "#51 — the concrete cut" and "Validation nets — WARM and SCRIPTED" sections) and the project memory (kronuz-extraction-backlog, kronuz-unstallable-event-loop, xapiand-docs-e2e-suite).

Where things stand: Kronuz/http is a complete generic HTTP server library (un-stallable offload + classification + watchdog, compression, conditional, range — all pushed). #51 (collapse Xapiand's search onto one http::HttpHandler) is underway: stage 1 and stage 2 are done and E2E-validated — every request-only helper is now off HttpClient (stage 2a 0aff727c8 content-neg; stage 2b/2c 42b227334 url_resolve/query_field_maker/encoding_http_response). The coupling map + staged plan are in the handoff.

The task: stage 3, the real architectural cut. Define SearchApplication : http::HttpHandler whose handle(const Request&, ResponseWriter&) does what prepare() + the chosen *_view do; move the DB-bound helpers (expand_paths/resolve_index_endpoints/node_obj/retrieve_database) behind it; split the transport (write_* that call this->write) into the writer seam; map the blocking-body model (request.wait()/pending CV, NDJSON next_object) onto BodySink and the runner+requests deque onto the per-reactor Dispatcher; rewire Xapiand's HttpServer to create http::HttpConnection instead of HttpClient. This is design, not a mechanical lift — go incrementally and keep it E2E-green at each step; prepare()'s method+URL-predicate dispatch isn't radix-clean so it stays a custom dispatch in handle(); catch_http_errors is already a free function to reuse at the boundary.

The nets are warm: our binary Xapiand/build/bin/xapiand (note build/bin/), baseline xapiand-master-bench/build/bin/xapiand @ 7bd295b0b, saved baseline report harness/results/e2e_base_7bd295b.json. The per-stage loop is scripted: cmake --build build -j then harness/e2e_check.sh (green = 0 regressions + bodydiff = exactly 3 volatile fields). Fast inner loop is single-TU -fsyntax-only via build/compile_commands.json. Gotcha: pkill -9 -f bin/xapiand before E2E runs (the scripts do this). Commit identity Germán Méndez Bravo <german.mb@gmail.com>; Xapiand commits stay local; Kronuz/http libs push to the Kronuz gh account; network needs dangerouslyDisableSandbox: true.

Some things might be half way done.
